# Donut (Document Understanding Transformer)

Donut is an OCR-free end-to-end Transformer model for document understanding. It maps a document image directly to a structured output (e.g., JSON) without requiring an intermediate OCR step.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python donut_ocr.py <path_to_image>
```
