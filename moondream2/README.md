# Moondream2

Moondream2 is a small, efficient vision-language model designed to run on a wide range of devices. Despite its size, it is surprisingly capable at image description and text extraction.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python moondream_ocr.py <path_to_image>
```
